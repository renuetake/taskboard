module CategoriesHelper
  # def get_categories(user_id)
  #   # current_user.categories.eachで取れることは取れるが、登録失敗した際にごみも取れてしまうので
  #   # 泣く泣くCategories.find_byで取る
  #   return Category.find_by(user_id: user_id)
  # end
end
